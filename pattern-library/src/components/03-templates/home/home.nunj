{% render '@site-header' %}

<main>

    {% render '@hero' %}

    {% if primaryPanel %}

        {% render '@panel', primaryPanel %}

    {% endif %}

    {% if secondaryPanel %}

        {% render '@panel', secondaryPanel %}

    {% endif %}

</main>
