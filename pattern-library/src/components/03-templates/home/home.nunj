{% render '@site-header', { home: true } %}

<main>

    {% render '@hero' %}

    {% if primaryPanel %}

        {% render '@panel', primaryPanel %}

    {% endif %}

    {% if secondaryPanel %}

        {% render '@panel', secondaryPanel %}

    {% endif %}

</main>

{% render '@site-footer' %}
