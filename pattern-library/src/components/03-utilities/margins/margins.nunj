{%  for size in ['small', 'medium', 'large'] %}

    <p class="u-mb-{{ size }}">The bottom margin on this text is set to the {{ size }} gutter size.</p>

{% endfor %}

<p class="u-mb-zero">The bottom margin on this text is set to zero.</p>

<p><strong>Note:</strong> use the font helper classes if you want to set the bottom margin to equal the element's line height (eg to match paragraph spacing).</p>