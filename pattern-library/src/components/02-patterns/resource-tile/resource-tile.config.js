const config = {
  context: {
    button: 'Report on safeguards',
    description: '<p>Patient information is sensitive and confidential. We will only be able to realise the huge potential and value of patient data in the NHS if both the public and clinicians can trust the system, and have confidence that data is appropriately collected and managed</p>',
    title: 'How is data kept safe?'
  },
  preview: '@preview-boxed'
}

module.exports = config
